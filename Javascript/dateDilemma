function parseDate(date) {
	if (/\d{4}*/.test(date)) {
	  console.log("year first")
	  // parse into y m d
	  // rearrange into yyyy-mm-dd
	  var split = test.split(/^d/)
	  console.log(split)
	}
}

parseDate("2012 3 17")